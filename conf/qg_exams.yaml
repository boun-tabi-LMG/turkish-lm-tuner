defaults:
  - default

dataset_name: exams
task: question_generation
max_input_length: 50
max_target_length: 200
training_params:
  num_train_epochs: 10
  per_device_train_batch_size: 16 # worked in ultramarine
  per_device_eval_batch_size: 16 # worked in ultramarine
  output_dir: ./t5_qg_exams
